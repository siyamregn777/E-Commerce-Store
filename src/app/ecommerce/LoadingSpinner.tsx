// src/app/ecommerce/LoadingSpinner.tsx
import { FaSpinner } from 'react-icons/fa';

const LoadingSpinner = () => (
  <div className="loading-spinner">
    <FaSpinner className="spinner-icon" />
  </div>
);

export default LoadingSpinner;
